import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-photo-gallery-template',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
